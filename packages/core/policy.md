# Execution policy
- live execution disabled by default
- mandatory approvals on every trade
- dry-run and shadow-order modes supported
- live mode requires secrets, funded wallets, and staging signoff
